import { createTheme } from "@mui/system";

const theme = createTheme({
  palette: {
    primary: {
      main: "#000",
    },
    secondary: {
      main: "#85DCBA",
    },
    third: {
      main: "#C38D9E",
    },
  },
});

export default theme;
